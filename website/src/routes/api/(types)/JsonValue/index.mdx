---
title: JsonValue
description: JSON value type.
contributors:
  - ksaurav24
---

import { Property } from '~/components';
import { properties } from './properties';

# JsonValue

JSON value type.

## Definition

- `JsonValue` <Property {...properties.JsonValue} />

> Since TypeScript has no finite-number type, `NaN`, `Infinity`, and `-Infinity` type-check as `JsonValue` even though the [`jsonValue`](/api/jsonValue/) schema rejects them at runtime.
